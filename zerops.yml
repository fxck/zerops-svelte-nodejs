zerops:
  - setup: sveltenode
    build:
      base: nodejs@20
      buildCommands:
        - npm i
        - npm run build
      deploy:
        - build
        - package.json
    run:
      start: npm start
